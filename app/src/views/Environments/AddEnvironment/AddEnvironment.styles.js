import styled from 'styled-components';

const StyledAddEnvironment = styled.div`
  .message {
    padding: 20px 0;
    text-align: center;
    color: var(--light-neutral);
  }
  .save-button {
    margin-top: 10px;
    padding: 0 30%;
  }
`;

export default StyledAddEnvironment;
